# Copie ce fichier en ".dev.vars" (sans .example) pour tester en local avec `wrangler dev`.
# Le fichier ".dev.vars" est ignoré par git (voir .gitignore) : il ne sera jamais poussé sur GitHub.
GEMINI_API_KEY=colle_ta_cle_ici
